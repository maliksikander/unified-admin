import { NotFoundComponent } from "./not-found.component";

describe("NotFoundComponent", () => {
  let fixture: NotFoundComponent;

  beforeEach(() => {
    fixture = new NotFoundComponent();
  });

  it("should create not found component", () => {
    expect(fixture).toBeTruthy();
  });
});
